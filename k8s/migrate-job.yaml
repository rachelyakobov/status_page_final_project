apiVersion: batch/v1
kind: Job
metadata:
  labels:
    owner: dr_admin
  name: statuspage-migrate-dr
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: migrate-dr
        image: 992382545251.dkr.ecr.us-east-1.amazonaws.com/dr_repo_statuspage:latest
        command: ["python","manage.py","migrate","--noinput"]
        envFrom:
        - secretRef:
            name: statuspage-secrets
  backoffLimit: 1
